import ScrollReveal from "scrollreveal";

const revealDefaultSetting: scrollReveal.ScrollRevealObjectOptions = {
  reset: true,
  delay: 400,
  duration: 500,
};

export default ScrollReveal(revealDefaultSetting);
